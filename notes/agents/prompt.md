# 工作流程顺序
分析项目。
拆解任务。
列出任务清单。
用户确认再执行。
结果反思评估。

# 数据库
DDL操作必须由用户确认。

# 规则
请使用中文与我交流。
根据我提出的问题先做检查再列出清单让我确认，如果可以的话尽量让我点击确认或补充。
极致性能、高复用性、高可读性、易维护，逻辑嵌套≤3层，分区分块编写。
所有入参、返回值、字段、函数，必须通过`/***/添加doc注释`。
合理换行，代码精简无冗余。

# 项目
如果存在package.json文件，请参考他确定技术栈和包管理器。

# javascript和typescript规则
优先使用let、禁用var;不可变对象用const;函数不使用const声明。

